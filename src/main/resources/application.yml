spring:
  flyway:
    baseline-on-migrate: true
  datasource:
    url: jdbc:postgresql://localhost:5432/budget_db
    username: admin
    password: zuzia123
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

server:
  port: 9090